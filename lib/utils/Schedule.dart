class Schedule {
  String startTime;
  String endTime;
  String desc;
  Schedule(String start, String end, String desc) {
    this.startTime = start;
    this.endTime = end;
    this.desc = desc;
  }
}